# Public client-side variables (bundled into the browser — never put secrets here)
# VITE_API_URL=https://api.example.com

# Server-only secrets (set in Vercel Project Settings → Environment Variables)
# DATABASE_URL=
# STRIPE_SECRET_KEY=
